# Tested CPU runtime pins for the JAX inference port.
#
# This is the current qualified CPU stack. CI separately retains the supported
# compatibility floor (JAX/jaxlib 0.4.30 + NumPy 1.26.4).
jax==0.6.2
jaxlib==0.6.2
numpy==2.2.6
scipy==1.15.3
librosa==0.10.2.post1
pandas==2.3.3
soundfile==0.12.1

# Development/test tooling and PyTorch are intentionally separate from the
# runtime. Install `.[test]`, `.[convert]`, or `.[parity]` as needed.
