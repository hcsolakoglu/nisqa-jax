# Tested CPU runtime pins for the JAX inference port.
#
# jax is pinned to the tested 0.4.30 minor (jax>=0.4.30,<0.5 in pyproject.toml).
# JAX's compilation-cache config API churns across minors — e.g.
# `jax.config.initialize_cache`/`is_initialized` were deprecated in 0.4.30 and
# removed in newer releases; this repo uses the version-tolerant
# `jax_compilation_cache_dir` knob, but staying within 0.4.x avoids surprise
# breakage of the persistent cache and JIT behavior. Bump only after re-running
# the full parity suite.
jax==0.4.30
jaxlib==0.4.30
numpy==1.26.4
scipy==1.11.4
librosa==0.10.2.post1
pandas==2.1.4
soundfile==0.12.1

# Development/test tooling and PyTorch are intentionally separate from the
# runtime. Install `.[test]`, `.[convert]`, or `.[parity]` as needed.
